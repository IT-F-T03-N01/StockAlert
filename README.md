# StockAlert
Community pharmacy inventory and expiry tracker with barcode scanning and supplier order management.

# Instructions

- clone the repository
`git clone <repo-url>`
- and cd into it
`cd StockAlert`
- run the following commands
-`git checkout -b develop`
- `ls` to view the files

run the necessary commands to grab the packages and run the project
`flutter pub get` ...
